ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        id = omsorgspenger-tilgangsstyring
        modules = [no.nav.omsorgspenger.AppKt.app]
    }
}
nav {
    auth {
        issuers = [{
            alias = "azure-v2"
            jwks_uri = ""
            jwks_uri = ${?AZURE_V2_JWKS_URI}
            issuer = ""
            issuer = ${?AZURE_V2_ISSUER}
        }]
    }
}
